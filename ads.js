window.KSL = window.KSL || {};
KSL.Ads = {
  getBannerHtml: function () {
    return ''; // Возвращаем пустоту вместо верстки баннера
  },
  bindEvents: async function (shadow) {
    // Ничего не делаем и не запускаем таймеры рекламы
    return; 
  }
};